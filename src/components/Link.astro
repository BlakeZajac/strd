---
interface Props {
    label: string;
    url: string;
    target?: string;
    className?: string;
}

const { label, url, target = "_self", className = "" } = Astro.props;

if (!url && !label) return;
---

<a href={url} target={target} class={`link ${className}`}>
    <span class="link__label">
        {label}
    </span>
</a>

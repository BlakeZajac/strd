---
interface Props {
    data: {
        years: Array<{
            year: number | string;
            items: Array<{
                title: string;
                author: string;
                url?: string;
            }>;
        }>;
    };
}

const { data } = Astro.props;
---

<div class="l-section">
    <div class="l-container">
        {
            data.years?.map((year) => (
                <div class="list-2col">
                    <div class="container-d-auto border-top">
                        <div class="list-title">{year?.year}</div>
                    </div>

                    <div class="container-d-75 is-list">
                        {year?.items?.map((item) => (
                            <div class="list-3col">
                                <div class="list-text">{item?.title}</div>

                                {item?.url ? (
                                    <a
                                        class="list-text"
                                        href={item?.url}
                                        target="_blank"
                                    >
                                        {item?.author}
                                    </a>
                                ) : (
                                    <div class="list-text">{item?.author}</div>
                                )}
                            </div>
                        ))}
                    </div>
                </div>
            ))
        }
    </div>
</div>
